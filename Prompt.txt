Go through the below documents fully, like all subpages, everything that you can sift through and answer my queries or help me with the scenarios 

https://gofastmcp.com/getting-started/welcome 

https://modelcontextprotocol.io/introduction

I would be using a gitlab hosted on a container in my local, i create 3 repos in there, a shared pipeline repo, which stores all of my CICD pipelines, a sample java project and a simple python project For the python and java project, i'll make ti trigger a webhook to say a docker container running on my local which will probably trigger an LLM flow, giving a basic prompt to say Chatgpt to find the details of pipeline and give me suggesstions or options to cure the failure, the chatgpt should then me consulting with my MCP server, again hosted on my laptop as a docker container to get the codebase and other context that it needs and give me a solution, that solution, if there are multiple, should be ranked by confidence scores again coming from LLM (in this case Chatgpt) only, if there are confidence scores say greater than 80% or 90%, then i maybe can give an option to the user to raise an MR wiht the fixes and post a manual review and merge it could be implemented All these communications, would be done via a streamlit UI chatbot APP (, which would be showing the details of the project, suggested fixes from LLM and their confidence scores (maybe not literally, but you could use words to show confidence), so a Slack bot is also needed which can interact with LLM and keep sessions as well for a time interval say 3 hours, the data base reuqired for it can again be maintained at my end through a docker container running locally on my laptop Let me know the feasibility of this and then we can talk it through

Now study the MCP documents i gave you at the start and develop an MCP APP for me which is accessible to any LLM via a docker container running on my desktop and contains gitlab functions needed for LLM to pull the context and get any f the required details

now go through this gist page throughly, this has a plan implemented via AWS Cloud, but as i already said, in my case it's going to be local, so if that's the case can you craft a plan for me based on the above conversations we had, obviously we'll have to eliminate the lambda functions and add them as containers to out local setup, but our model needs to be better in all terms from the reference that i am sharing and less complex Below is the github gist link

https://gist.github.com/nitinprabhakaran/9ed8a086734872b40779000797e1540b


How would the connection from the docker containers running on my laptop to LLM, would look like, could you guide me on that setup, like how would that work ?


i do not need a slack bot instead, it is to be replaced by a chatbot built on streamlit UI which should have adaptive cards, ability to differentiate between text and code snippets, also buttons to clikc on for actions like creating MR and all


I do not need a monitoring solution for now, that may come later, i would be using claude desktop as LLM


I won't be giving prompts initially like Analyze pipeline 12345 in project 1, it should be more kind of proactive, like it's tagging the commit author and send a message on the chatbot, let me know if it's possible witht he current setup or not


Also want to introduce a snooze feature, wherein, if a pipeline failure occurred and say reported to the chatbot, it has i also have a capability of passing a message say snooze for 8 hours, then that project and that particular branch related alerts should be snoozed for 8 hours


Gitlab MCP Server, i need to talkt o you about that, MCP server's logic should focus on getting proper context and sending it to LLM, at first the context can be just the diff code in the commit and the previosu commit, assuming that the earlier pipeline for the same branch was successful, if LLM is not confident about it, then it should ask for more context like the whole file where diff was present, even if that's not sufficient then LLM should ask for the whole codebase, if it still needs values of CICD varoables defined in gitlab MCP server should be capable enough to fetch it and provide it to LLM


Get more ideas from the github gist document that i gave you and one thing, the confidence score should also be coming from LLM and not the MCP server, so LLM and MCP server should have communications about information, i would need that sort of MCP server


I want Sonarqube to be implemented in this structure as well


Below are some references for gitlab mcp server, focus on my usecase and only take reference from these


https://mcp.so/server/gitlab

https://github.com/zereight/gitlab-mcp

https://gitlab.com/fforster/gitlab-mcp


Below are some sonarqube MCP servers


https://github.com/SonarSource/sonarqube-mcp-server

https://github.com/sapientpants/sonarqube-mcp-server


MCP servers in my case are to be coded in python


Build a nice and good Streamlit UI chatbot app with adaptive cards, buttons and all, maybe separates panes for Sonarqube and Gitlab failures, and seperates sessions could maybe show up as separate chats in streamlit UI


For Sonarqube, the recent analysis should be displayed and fixes suggessted around that


Is Sonarqube cloud free for features like quality gates and all, if not, let's host it on a docker container on local and use that for analysis and quality gates, include that as well in the plan


Once the all of this is done, instead of a script giveme manual instruction to set up the project and make ti working, like which files are to be put where and all kind of things that i would rquire


Also make sure that in MCP servers, we are not using any hard-coded patterns to determine the confidence score in any manner and all of that is coming from LLM, same goes for the fix (both in gitlab and sonarqube) 

This should be a proactive approach, like failures should be detected and webhook is triggered which should be an invoking point for the system and LLM should display the things on chat console with suggested fixes

I am using a MAC arm64 with about 10GB RAM which will be alloted to colima and yet having about 15GBs of RAM for core operations, so keep that in mind which recommedning containers or images or tools

Do not created setup script, i want to set everything up manually, just give me the instruction as to which file goes where and the directory layout and manul setup instructions and i should be able to take it